Vuejs
  語法嚴格，對空格嚴格限制，對組件component命名規則有限制
  組件注冊引用命名規則，如有“-”在編譯時會忽略，并且將傳入的參數命名全部小寫
  函數括號後面必須加空格,屬性值後面不需要，組件命名官方建議針對一個模塊的所有組件使用相同命名標識
  如：UserMenu,UserList..,在組件引用時采用不同唯一標識，為的是區別相同的組建類型不同的功能組建，

  數據動態綁定v-bind
 關於父子級模塊數據傳輸，子模塊提供props接收參數定義字段類型，可以接收數組，對象（props:{objName:{type:Object}})，
        在父級模塊中如<child objName="data"></child>
        如在父級調用子級的數據,使用parent


